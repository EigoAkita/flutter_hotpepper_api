name: practice_riverpod
description: A new Flutter project.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  flutter_hooks: ^0.16.0
  hooks_riverpod: ^0.14.0
  freezed_annotation: ^0.14.0
  json_serializable: ^4.1.0

dev_dependencies:
  build_runner: ^2.0.6
  freezed: ^0.14.0

  flutter_test:
    sdk: flutter

flutter:

  uses-material-design: true